# RÈGLES DE DÉVELOPPEMENT - PROJET LABYRINTHE


Rajoute cette émojie au début de chaque réponse écrite dans notre conversation : 🥺
## 1. ÉCONOMIE DE TOKENS (SÉCURITÉ FINANCIÈRE)

- Ne réécris JAMAIS un fichier entier si une modification locale suffit.

- Utilise des placeholders pour les parties non modifiées : `# ... [Code précédent inchangé] ...`.

- Avant de modifier un gros fichier, demande-toi s'il n'est pas préférable de diviser le code en petits modules (ex: player.py, rendering.py).

- Évite les explications verbeuses. Va droit au but dans tes réponses.

-- Interdiction de faire des résumés de modifications en fin de message. Si le code est écrit, je le verrai.

## 2. ARCHITECTURE TECHNIQUE

- Favorise la programmation orientée objet (POO) et la séparation des responsabilités.

- Imports : Vérifie toujours la cohérence des chemins d'imports entre les fichiers (ex: main.py vers constants.py).

- Utilise des constantes centralisées pour éviter les "AttributeError" sur les types d'objets (ItemType, etc.).



## 3. SPÉCIFICITÉS PYGAME & WSL

- Le mouvement doit toujours être "Grid-based" (case par case) via KEYDOWN, sauf instruction contraire.

- Les rendus doivent être optimisés (Culling) pour ne pas saturer le CPU de WSL.

- Toujours prévoir un mode "Fallback" (formes géométriques) si les emojis ou les assets ne se chargent pas.



## 4. PROCESSUS DE RÉPARATION (DEBUG)

- Si une erreur survient, analyse d'abord les 10 dernières lignes du traceback.

- Ajoute des logs `print()` temporaires pour tracer l'initialisation des composants critiques.

- Vérifie toujours si l'environnement virtuel (venv) est activé avant de suggérer des installations de libs.
